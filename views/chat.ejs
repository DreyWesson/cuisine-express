<div class="container">
  <% if (currentUser) { %>
  <h1>Chat</h1>
  <form id="chatForm" class="form-group">
    <div class="input-group mb-3">
      <input
        id="chat-input"
        type="text"
        class="form-control"
        placeholder="Enter message"
        aria-label="Enter message"
        aria-describedby="message"
      />
      <input
        id="chat-user-name"
        type="hidden"
        value="<%= currentUser.fullName %>"
      />
      <input id="chat-user-id" type="hidden" value="<%= currentUser._id %>" />
      <button
        class="input-group-append btn btn-primary"
        type="submit"
        id="message"
      >
        Send
      </button>
    </div>
  </form>
  <div
    id="chat"
    style="min-height: 400px; min-width: 400px; border: 1px solid #ced4da;"
  ></div>
  <% } %>
</div>
